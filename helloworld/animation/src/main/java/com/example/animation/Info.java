package com.example.animation;

public class Info {
    /**
     * 1、帧动画：电影放映式 20帧 gif动画 淡入淡出动画
     *
     * 2、补间动画：坐标未动 动画开始和结尾的中间过程都是假象，是渲染出来的表象，只是显示的位置变动，View的实际位置未改变
     * 具体效果，包括平移（TranslateAnimation）、缩放(ScaleAnimation)、旋转(RotateAnimation)、
     * 透明度(AlphaAnimation)四个子类 摇摆动画、集合动画、轮播图
     *
     * 3、属性动画：不断地控制控件的属性变化达到动画的效果
     *
     * 4、矢量动画：bitmap通过每个像素点上存储色彩信息来表达图像，而SVG是一个绘图标准，与之相对，
     * 最大的优势是SVG放大不会失真，而且bitmap需要不同分辨率适配，SVG不需要
     * 5、实现手段：
     *
     */
}
